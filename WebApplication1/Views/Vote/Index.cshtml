@model IList<WebApplication1.Models.ViewModels.CandidateViewModel>
@{
    ViewData["Title"] = "Index";
}


<partial name="_votingScenarioStyle" />
<h1 class="text-center" id="voting-title">Select your choice wisely</h1>
<br />
<div class="voting-container">

    @{
        string id = "";
    }
    @foreach (var candidate in Model)
    {
        @if (candidate.isNeutralOpinion == true)
        {
            id = "neutral-candidate";
        }
        <div class="candidate-container" id="@id">
            <p class="hidden-candidateId" hidden>@candidate.Id</p>
            <p class="candidate-name"><i class="fa fa-check-circle-o" aria-hidden="true"></i> @candidate.FirstName @candidate.LastName</p>
            <p>@candidate.StateName</p>
        </div>
    }

    <button type="button" id="vote-button" title="Validate Vote">
        <i class="fa fa-rocket" aria-hidden="true"></i>
    </button>
</div>

<partial name="_votingScenarioScript" />




<style>
    #results-container {
    display: none/*flex*/;
    flex-direction: column;
    justify-content: center;
    text-align: center;
}
    .margins {
    margin-top: 3%;
    margin-bottom: 3%;
}

    
.one-result-container {
    text-align: center;
    font-size: 20px;
    display: flex;
    flex-direction: row;
    justify-content: center;
    align-items: center; /*in order to vertically center the cup icon*/
        width: 50%;
        margin-left: auto;
        margin-right: auto;
        margin-top: 1%;
        margin-bottom: 1%;
    }

    .one-result-container .rank-container {
        /*margin-left: 1%;
            margin-right: 1%;*/
        min-width: 50px;
    }

    .one-result-container .candidate-data-container {
        width: 250px;
    }

        .one-result-container .candidate-data-container p {
            margin-bottom: 0%;
            margin-top: 0%;
        }

    .one-result-container span {
        font-size: 250%;
    }
    #voting-spinner{
        display:none;
    }
</style>


<div id="results-container" class="margins">
    <h1 class="margins">Current Results</h1>
    
</div>
<div class="spinner-border" id="voting-spinner">

</div>

    @*int i = 0;
        <div id="results-container" class="margins">
            <h3 class="margins">Current Results</h3>

            @foreach (var candidate in Model.Candidates)
            {
                i++;
                //so it is the first candidate, lets add the cup icon to it
                <div class="one-result-container">
                    <div class="rank-container">
                        @if (i == 1)
                        {
                            <span><i class="fa fa-trophy" aria-hidden="true"></i></span>
                        }
                        else
                        {
                            <span>@i.ToString()</span>
                        }

                    </div>
                    <div class="candidate-data-container">
                        <p>@candidate.FirstName @candidate.LastName</p>
                        <p>@candidate.VotesCount Votes</p>
                    </div>
                </div>
            }
        </div>*@
